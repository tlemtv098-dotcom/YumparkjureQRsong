[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn yum_jukebox.wsgi:application --bind 0.0.0.0:\"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
