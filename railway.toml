[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn yum_jukebox.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/healthz/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
